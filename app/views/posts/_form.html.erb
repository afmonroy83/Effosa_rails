<!-- start banner Area -->
			<section class="banner-area relative" id="home">
				<div class="overlay overlay-bg"></div>
				<div class="container">
					<div class="row d-flex align-items-center justify-content-center">
						<div class="about-content col-lg-12">
							<h1 class="text-white">
								Añadir blog
							</h1>
							<p class="text-white link-nav"><a href="index.html">Inicio </a>  <span class="lnr lnr-arrow-right"></span>  <a href="contact.html"> Contactenos</a></p>
						</div>
					</div>
				</div>
			</section>
			<!-- End banner Area -->

/
			<!-- End contact-page Area -->
<%= form_for  post do |f| %>
  <div>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div>
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

	<div>
	    <%= f.label :attachments %>
	    <%= f.file_field :attachments, multiple: true %>
	</div>

  <%= f.submit %>
<% end %>
<%= render 'shared/errors', object: post %>
